<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hr.mapper.PermissionMapper">

    <resultMap id="PermissionResultMap" type="com.hr.entity.Permission">
        <id property="id" column="id"/>
        <result property="permName" column="perm_name"/>
        <result property="permCode" column="perm_code"/>
        <result property="permType" column="perm_type"/>
        <result property="parentId" column="parent_id"/>
        <result property="path" column="path"/>
        <result property="icon" column="icon"/>
        <result property="sort" column="sort"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 根据角色 ID 查询权限列表 -->
    <select id="selectByRoleId" resultMap="PermissionResultMap">
        SELECT p.*
        FROM permission p
        INNER JOIN role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId}
        ORDER BY p.sort
    </select>

    <!-- 根据用户 ID 查询权限列表 -->
    <select id="selectByUserId" resultMap="PermissionResultMap">
        SELECT DISTINCT p.*
        FROM permission p
        INNER JOIN role_permission rp ON p.id = rp.permission_id
        INNER JOIN user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
        ORDER BY p.sort
    </select>

</mapper>
